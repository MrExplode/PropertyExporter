#PropertyExporter
language: java
before_script:
   - sudo add-apt-repository ppa:deadsnakes/ppa
   - sudo apt-get update
   - sudo apt-get install python3.6
script:
   - scripts/artifact.sh
   - scripts/javadoc.sh